server:
  port: 1010

spring:
#  application:
#    name: configdemo
##  config:
#    import: optional:configserver:http://localhost:8888
#  profiles:
#    active: native
management:
    endpoints:
      web:
        exposure:
          include: refresh

build:
  id: ${ID:101}
  version: ${VERSION:1.0.0}
  name: ${NAME:Default Build}

